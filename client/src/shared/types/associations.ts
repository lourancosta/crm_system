// Kept in sync with src/lib/objectTypes.ts's AssociableType on the backend —
// the set of record types the generic /api/associations endpoint accepts.
export type AssociableType = 'contacts' | 'companies' | 'deals' | 'tickets' | 'partnerships' | 'licenses' | 'invoices';
